# Matriz De Cumplimiento - IntegrApex

Fuente: `Historias_Usuario.xlsx` (criterios de aceptación leídos directamente de cada hoja, no resumidos). Estado verificado criterio por criterio contra el código actual: `backend/src/routes/*.routes.js`, `frontend/public/app_v99.js` y `frontend/public/js/modules/*.js`.

Leyenda: **Cumple** (los 3 criterios se satisfacen), **Cumple con matices** (funciona pero con una desviación menor respecto a la letra del criterio), **Parcial** (falta al menos un criterio de forma sustancial).

## Acceso (Cumple 100%)

| Historia | Estado | Evidencia | Notas |
|---|---|---|---|
| HU-00 Inicio de sesión por rol | Cumple | `auth.routes.js` `/auth/login` | Verificado en navegador: bloquea vacíos/incorrectos, deduce rol sin pedirlo, cada registro guarda `creado_por_id/nombre/en`. |
| Registro de usuario con aprobación | Cumple | `auth.routes.js` `/auth/register`, `/admin/requests`, `/admin/approve` | Verificado en navegador extremo a extremo: cuenta pendiente → login bloqueado con aviso → aprobación por dependencia con `aprobado_por/aprobado_en` → login habilitado. |
| Por Firmar | Cumple | `bitacora.routes.js` `/bitacora/por-firmar`, `/bitacora/:id/firmar` | Cada firmante ve solo lo suyo; firma solo con su propio `user.id`; se marca `completada` cuando firman todos. **Nota:** la pantalla ya funcionaba pero no tenía ningún enlace en el sidebar — era inalcanzable desde la UI. Se conectó al agregar el tablero de HU-17. |

## Contrato y expediente

| Historia | Estado | Notas |
|---|---|---|
| HU-01 Alta de contratos | Cumple | Folio único, suma catálogo = monto, cantidades/precios > 0, programa dentro de plazo, garantías con monto, PDF inmutable (no existe endpoint para reemplazarlo). |
| HU-02 Fianzas y garantías | **Cumple** | Póliza con afianzadora/vigencia/monto y PDF consultable: sí. **El sistema ahora emite una alerta real a 30/15/5 días** (umbrales configurables via `umbrales_alerta`): un job (`backend/src/jobs/alertasScheduler.js`) corre al arrancar el servidor, cada hora, y ademas se evalua al vuelo justo despues de registrar una fianza o un endoso, insertando una notificación real en `notificaciones` (rol `dependencia`) la primera vez que la vigencia entra en cada umbral — deduplicado por fianza vía `alertas_emitidas`, y reiniciado cuando un endoso renueva la vigencia. Verificado extremo a extremo: fianza con vigencia a 3 días genera 3 notificaciones reales (30/15/5) visibles en la campana de notificaciones. |
| HU-03 Convenios modificatorios | Cumple | Nueva versión de catálogo/programa sin alterar la anterior, clasifica Art. 59 / 59 Bis automáticamente, histórico con fecha/autor/motivo y endosos generados. |
| HU-04 Expediente contractual | **Cumple** | Los 5 bloques existen y el buscador aplica AND (folio, contratista, objeto, periodo, tipo). Los bloques siguen repartidos en pestañas, no en una sola vista (interpretación de diseño, no bloqueante — la consulta integrada real es el propio buscador, que sí cruza los 5 bloques). **Cada fila de resultado del buscador ahora trae su propio enlace de descarga** (`archivo`, resuelto por bloque: `pdf_contrato`, `documentos.path`, `fianzas.pdf_poliza`, `minutas.pdf_path`) cuando el registro tiene un archivo real asociado. |

## Programa, avance y bitácora

| Historia | Estado | Notas |
|---|---|---|
| HU-05 Programa y curva de avance | Cumple | Gantt concepto×periodo con color ejecutado/no ejecutado, 3 curvas (programado/ejecutado/financiero), filtros por concepto y periodo recalculan ambas vistas, % global y por concepto. |
| HU-06 Trabajos terminados por periodo | Cumple | Cantidad ligada a concepto y a nota de bitácora, acumulado en vivo, bloqueo Art. 118 RLOPSRM. Nota menor: no valida que la nota vinculada sea tipo "Entrega"/"Avance" (acepta cualquier tipo de nota del contrato). |
| HU-07 Alertas de atraso por concepto | **Cumple** | Crear/pausar/eliminar sin afectar otras: sí. **"La alerta dispara" ahora es un evento real de backend**, no solo un badge recalculado al abrir la pestaña: `checkAlertasConcepto()` (mismo job de HU-02) se evalúa tras cada evento que cambia el avance real del contrato (registrar trabajos por periodo, integrar una estimación) y tras crear/editar una alerta, comparando el avance real por concepto (`backend/src/utils/avanceConceptos.js`, misma fuente que el bloqueo Art. 118 de HU-06) contra el umbral configurado — dispara (y notifica) solo la primera vez que cruza por debajo, y se "rearma" si el avance vuelve a superar el umbral. **La notificación ahora sí se entrega por el canal elegido**: `canal=sistema` va a la campana de notificaciones de la app; `canal=correo` va a un buzón de correo simulado (`GET /api/correos-salientes`) en vez de mezclarse con la bandeja del sistema — sin depender de credenciales SMTP reales en este prototipo. Verificado extremo a extremo: alerta con umbral 50% y avance real 0% dispara de inmediato y aparece en el canal correcto. |
| HU-08 Apertura formal de bitácora | Cumple | Bitácora única, firmantes tomados del equipo del contrato (no texto libre), firma completa cuando firman todos, primera nota con los datos obligatorios. |
| HU-09 Notas tipificadas con firma | **Cumple** | Tipos por rol, folio correlativo, firma (hash) desde la cuenta propia, vínculo opcional a nota previa, no hay endpoint de edición/borrado (inmutable de facto). **El formato "dice / debe decir" ahora es un campo estructurado dedicado**: al marcar "Es una corrección formal" en el formulario de nueva nota, se piden dos campos — "Dice" (autocompletado desde el contenido real de la nota vinculada) y "Debe decir" — y el backend valida que la nota original exista, guarda `correccion: {dice, debe_decir, nota_original_folio}` sin alterar la nota original, y compone el contenido de forma consistente. El listado muestra la corrección con un badge dedicado y el texto "Dice" tachado junto al "Debe decir". |
| HU-10 Consulta y búsqueda de notas | **Cumple** | Filtra con AND por tipo, fecha inicio, fecha fin, **firmante** (select poblado con los autores reales de la bitácora) y **vínculo** (por folio), todos combinables. Cada nota tiene checkbox de selección + "Seleccionar todas"; **"Exportar selección" genera un .xlsx real** (vía `exceljs`, endpoint `GET /contratos/:id/bitacora/notas/export?ids=...`) — verificado que el archivo descargado es un ZIP/XLSX válido (firma `PK`), no un CSV disfrazado. Utilidad reutilizable en `backend/src/utils/xlsxExport.js` para futuras exportaciones (HU-19). |
| HU-11 Minutas, visitas y acuerdos | **Cumple** | Filtro por periodo (mes) sobre minutas y visitas ya cargadas del contrato — filtrado en cliente ya que ambas colecciones vienen embebidas en el contrato, sin round-trip extra. La nota de bitácora ahora acepta `referencia_tipo`/`referencia_id` (`minuta` o `visita`), validado contra el contrato en el backend, con selector en el formulario de nueva nota y badge visible en el listado. |

## Estimaciones

| Historia | Estado | Notas |
|---|---|---|
| HU-12 Apertura e integración de estimación | **Cumple** | Cálculo automático de anticipo/retención 5 al millar/deductivas: correcto. Bloqueo por excedente Art. 118: correcto. **Registro fotográfico y soportes ahora se cargan al momento de integrar** (`upload.fields` multipart en `/contratos/:id/estimaciones/integrar`, arrays `fotos`/`soportes` persistidos con `{path, nombre}`, visibles en el detalle bajo "Anexos del Periodo"). **Las notas vinculadas se seleccionan desde un buscador real** de la bitácora del contrato (filtro por texto en vivo, checkboxes, contador de seleccionadas), ya no un ID manual — el backend valida que cada nota pertenezca al contrato antes de guardarla. |
| HU-13 Envío de la estimación | **Cumple** | Fecha/hora de recepción registrada. El botón "Enviar" ahora **se deshabilita en la UI** cuando ya vencieron los 6 días naturales (antes solo fallaba después, en el backend). Al enviar se generan **notificaciones reales** en `notificaciones` para `supervision` y `residente` (verificado que se crean exactamente 2 registros). El plazo de 15 días de revisión ahora se calcula y se muestra como **semáforo** (verde/ámbar/rojo) en la carátula de la estimación, vía `backend/src/utils/plazosLegales.js`. |
| HU-14 Historial de estimaciones | **Cumple** | Filtros por periodo y estado (AND) agregados a `GET /contratos/:id/estimaciones` + formulario en la pantalla de Estimaciones. Verificado que un periodo inexistente devuelve vacío y que un periodo real devuelve exactamente esa estimación. |
| HU-15 Recepción, revisión y autorización | **Cumple** | ✅ Turnado secuencial obligatorio corregido (residencia no puede resolver antes de `en_revision`). ✅ Semáforo de 15 días visible para supervisión/residencia en la carátula (mismo helper que HU-13). ✅ **Revisión sección por sección** (carátula/generadores/fotos/soportes/notas): el modal "Turnar a Residencia" ahora es un formulario dinámico de filas, cada observación con `sección` + `concepto` (opcional, tomado del catálogo real del contrato) + `tipo` + `severidad` + `comentario`; el backend valida cada fila contra las 5 secciones válidas. La vista de estimación rechazada muestra badges de sección/tipo/severidad por observación. |
| HU-16 Reingreso tras rechazo | **Cumple** | La nueva versión queda como bloque independiente vinculado a la rechazada (`estimacion_vinculada_id`), sin reiniciar fechas de periodo. Las observaciones ahora se muestran inline en la estimación rechazada y se descargan en **Excel o PDF reales** desde `GET /estimaciones/:id/observaciones/export` (reutiliza los builders de HU-10/19). |
| HU-17 Tablero de estimaciones | **Cumple** | Nueva pantalla `Tablero de Estimaciones` (sidebar, HU-17) para residente/contratista/supervision/finanzas: solo activas (excluye rechazada/borrador), tarjetas de resumen agregado por estado (conteo + monto), **línea de tiempo por estimación** (reconstruida de los timestamps ya existentes: creación, presentación, revisión, autorización, instrucción de pago, pago), y panel **"Mis Pendientes"** filtrado por `requiere_mi_accion` (ahora también cubre a contratista cuando una estimación queda `autorizada`, antes solo se calculaba para supervision/residente/finanzas). Clic en una fila navega directo al detalle de esa estimación en su contrato. De paso se conectó **"Por Firmar"** al sidebar (existía y funcionaba, pero no había ningún enlace para llegar a esa pantalla). |
| HU-18 Portafolio ejecutivo | **Cumple** | Pantalla dedicada "Portafolio Ejecutivo" (antes vivía mezclado dentro de "Listado de Contratos"). El semáforo ya combina los **3 factores reales**: avance físico vs. `avance_programado` calculado del programa de obra real del contrato a la fecha de hoy (ya no `60.0` fijo), atrasos en plazos legales (estimaciones con plazo de revisión/pago vencido + fianzas vencidas, reutilizando `plazosLegales.js`) y pendientes reales (estimaciones en proceso + bitácora sin firmar, no "todas las notas"). **Doble clic** abre un modal de detalle consolidado (físico/financiero/atrasos/penalizaciones); un clic simple sigue llevando al Expediente. **Agrupación** por contratista, ejercicio fiscal o tipo de contratación (usando `modalidad_pago` como proxy, ya que el modelo de datos no tiene un campo dedicado de tipo de contratación). **Comparativo contra el mes anterior** con flecha de tendencia, calculado con fechas reales de pago. |
| HU-19 Exportación de los 7 reportes | **Cumple** | Cada uno de los 7 reportes se descarga en **Excel real** (`exceljs`) o **PDF real** (`pdfkit`, tabla paginada con encabezado), a elección del usuario, desde `GET /contratos/:id/reportes/:tipo/export?formato=xlsx\|pdf`. **Selector de periodo** (acumulado / mensual / trimestral) filtra las estimaciones, notas y convenios subyacentes por fecha sin cambiar la estructura del reporte — verificado que un mes sin datos devuelve un archivo válido con 0 filas y el mes real devuelve la información esperada. Definiciones de columnas centralizadas en `backend/src/utils/reportData.js`, reutilizando `xlsxExport.js` (de HU-10) y el nuevo `pdfExport.js`. |
| HU-20 Tránsito a pago | **Cumple** | La suficiencia presupuestal ahora se calcula **contra el monto contractual real del contrato específico** (monto menos lo ya comprometido en otras estimaciones autorizadas/en pago/pagadas), no un techo global compartido — verificado que `disponible` ya no devuelve el `$15,000,000` fijo. Exige factura y CFDI: sí. **Valida la fianza de cumplimiento** cuando existe una registrada: bloquea la instrucción de pago si está vencida y permite continuar tras renovarla vía endoso (HU-02) — probado extremo a extremo (bloqueo → endoso de renovación → instrucción exitosa). **Semáforo de 20 días** visible en la carátula, y se genera notificación real a `finanzas` al emitir la instrucción. |
| HU-21 Registro del pago efectuado | Cumple | Marca la estimación como pagada, guarda fecha/importe/referencia/usuario; el avance financiero se deriva automáticamente de las estimaciones en estado `pagada` (no requiere un paso extra). |

## Resumen priorizado para la siguiente vuelta

1. ~~Bug real: HU-15 permitía que residencia resolviera sin que supervisión hubiera turnado~~ — **corregido y verificado.**
2. ~~HU-10: exportación a Excel falsa y filtros por firmante/vínculo faltantes~~ — **corregido y verificado** (exportación real a `.xlsx` con `exceljs`, filtros completos con AND).
3. ~~HU-17: backend listo pero sin pantalla~~ — **corregido y verificado** (tablero + Mis Pendientes + línea de tiempo; de paso se conectó "Por Firmar", que tampoco tenía enlace).
4. ~~HU-19: solo CSV, sin selector de periodo~~ — **corregido y verificado** (Excel y PDF reales para los 7 reportes, filtro acumulado/mensual/trimestral).
5. ~~HU-12: sin fotos/soportes al integrar, notas vinculadas por ID manual~~ — **corregido y verificado** (multipart real, buscador de notas real).
6. ~~HU-15: revisión sin secciones/tipo/severidad~~ — **corregido y verificado** (formulario dinámico seccionado).

Con esto quedan cerradas las 8 historias que estaban en "Parcial". Solo restan las 4 en "Cumple con matices" (HU-02, HU-04, HU-07, HU-09), fuera de la ruta crítica — ver sección "Fuera de este plan" abajo.

## Plan De Cierre (orden correcto para las 8 historias "Parcial")

Quedaban 8 historias en **Parcial**: HU-11, HU-12, HU-13, HU-14, HU-15, HU-16, HU-18, HU-20. No se resuelven en el orden en que aparecen en el Excel — hay dependencias reales de datos y de infraestructura entre ellas que conviene respetar para no reconstruir lo mismo dos veces.

### Bloque 1 — Ganancias rapidas e independientes (cualquier orden entre si) — ✅ Completado

Reutilizaron infraestructura que ya existia (`xlsxExport.js`/`pdfExport.js` de HU-10/19, el patron de filtros AND de HU-10). No dependian de nada mas y no bloqueaban nada mas.

1. ~~**HU-16**: endpoint para descargar las observaciones de una estimacion rechazada en PDF/Excel~~ — hecho, reutilizando los builders de HU-19, mas visibilidad inline de las observaciones (antes no se mostraban en ningun lado).
2. ~~**HU-14**: filtros por periodo y estado (AND) en `GET /contratos/:id/estimaciones` + UI~~ — hecho, mismo patron que HU-10.
3. ~~**HU-11**: filtro por periodo en minutas/visitas, y referencia opcional (`referencia_tipo`/`referencia_id`) en la nota de bitacora~~ — hecho, con selector en el formulario y badge en el listado.

Verificado en navegador (11/11): observaciones visibles + descarga real en Excel y PDF, filtro de periodo en historial de estimaciones (vacio vs. coincidencia exacta), filtro de estado, filtro por periodo en minutas/visitas, y nota nueva referenciando una minuta con su badge visible. Suites de regresion previas re-verificadas sin hallazgos nuevos.

### Bloque 2 — Pieza compartida de plazos legales + notificaciones — ✅ Completado

4. ~~Crear `backend/src/utils/plazosLegales.js`~~ — hecho: helper puro (fecha inicio + dias limite -> dias restantes + semaforo verde/ambar/rojo), reutilizado por los 3 puntos siguientes.
5. ~~**HU-13**: semaforo de 15 dias, deshabilitar boton "Enviar", notificacion real~~ — hecho. Verificado que se crean exactamente 2 notificaciones (supervision + residente) al enviar, y que el boton se deshabilita cuando ya vencieron los 6 dias.
6. ~~**HU-20**: semaforo de 20 dias, presupuesto real por contrato, fianza de cumplimiento~~ — hecho. Probado extremo a extremo: fianza vencida bloquea la instruccion de pago, un endoso de renovacion (HU-02) la desbloquea.
7. ~~**HU-15 (parte A)**: semaforo de 15 dias visible para supervision/residencia~~ — hecho, mismo helper. La otra mitad (revision seccionada) sigue en el Bloque 4, depende de que HU-12 exista primero.

Verificado en navegador y por API (Playwright + curl): semaforos visibles en la caratula de la estimacion con los colores correctos, notificaciones reales confirmadas leyendo la coleccion directamente, presupuesto real distinto del techo global anterior, y el ciclo completo fianza-vencida → bloqueo → endoso → instruccion exitosa. Suites de regresion previas (HU-10, HU-11, HU-14, HU-16, HU-17, HU-19, accesos por rol) re-verificadas sin hallazgos nuevos.

### Bloque 3 — HU-18 se apoya en todo lo anterior — ✅ Completado

8. ~~**HU-18**: avance programado real, atrasos reales, pendientes reales, agrupacion y comparativo~~ — hecho. Pantalla propia "Portafolio Ejecutivo" separada del Listado de Contratos generico. Semaforo de 3 factores real (avance programado calculado del programa de obra a la fecha de hoy, atrasos via `plazosLegales.js`, pendientes reales en vez de conteo de notas). Doble clic (deteccion manual, no el evento nativo `dblclick` que corre en paralelo con `click` y por eso navegaba de mas) abre detalle consolidado. Agrupacion y comparativo contra el mes anterior con datos reales.

Verificado en navegador (9/9): 6 tarjetas con semaforo real, comparativo visible, agrupacion por contratista funcional, doble clic abre el modal consolidado con los 4 indicadores pedidos, clic simple sigue navegando al expediente, y el Listado de Contratos genérico ya no duplica el semaforo. Las 5 suites de regresion previas (HU-10/11/14/16/17/19, roles) se re-corrieron limpias.

### Bloque 4 — HU-12 al final, y con el HU-15 completo — ✅ Completado

9. ~~**HU-12**: registro fotografico, soportes al momento de integrar (hoy solo se cargan al enviar, HU-13), y reemplazar el campo manual de ID de nota por un selector real que use el buscador de bitacora de HU-10~~ — hecho. `/contratos/:id/estimaciones/integrar` acepta multipart (`fotos[]`, `soportes[]`, hasta 10 cada uno) y valida que las `notas_vinculadas_ids` pertenezcan al contrato. El modal de integracion gano inputs de archivo reales y un buscador de notas en vivo (filtro por texto, checkboxes, contador), reemplazando el campo numerico manual. El detalle de la estimacion muestra un panel "Anexos del Periodo" con las fotos/soportes/notas vinculadas.
10. ~~**HU-15 (parte B)**: revision seccion por seccion (caratula/generadores/fotos/soportes/notas) con tipo/severidad/concepto por observacion~~ — hecho. `/estimaciones/:id/revisar` valida cada observacion contra las 5 secciones validas y guarda `{seccion, concepto, tipo, severidad, comentario}`. El modal "Turnar a Residencia" es ahora un formulario dinamico (agregar/quitar filas), cada fila con selects de seccion/concepto(catalogo real)/tipo/severidad. La vista de estimacion rechazada muestra badges por seccion/tipo/severidad, y el export de HU-16 incluye las columnas nuevas.

Es la pieza mas grande y la que mas toca la pantalla de integracion de estimaciones (dinero real), por eso quedo al final: para entonces ya existian y estaban probados el helper de plazos, las notificaciones reales y las exportaciones — se reduce el riesgo de tocar el formulario mas sensible del sistema con la infraestructura de soporte todavia sin probar.

Verificado en navegador (18/18): fotos y soportes reales persistidos y visibles en "Anexos del Periodo", buscador de notas con filtro funcional y seleccion persistente, 2 observaciones seccionadas (generadores/Alta, fotos/Critica) guardadas con sus 4 atributos, vista de rechazo mostrando las secciones/severidades, y el export de observaciones (HU-16) generando un `.xlsx` real con las columnas nuevas. Las 8 suites de regresion previas (`verify.js`, `verify_fixes.js`, HU-10, HU-17, HU-18, HU-19, Bloque 1, Bloque 2 — 90 checks en total) se re-corrieron sobre el codigo final sin hallazgos nuevos; los 2 unicos "fallos" en la primera pasada fueron falta de datos de prueba (el tablero HU-17 sin ninguna estimacion activa tras dejar la unica estimacion de la base en `rechazada`, resuelto sembrando una estimacion adicional) y un selector obsoleto en `test_bloque1.js` (`#turn-obs`, el textarea unico que el Bloque 4 reemplazo por el formulario seccionado) — ambos confirmados como artefactos de datos/script de prueba, no regresiones de la app.

### Bloque 5 — Pulido fino (HU-02, HU-04, HU-07, HU-09) — ✅ Completado

Las 4 historias que quedaban en "Cumple con matices" compartían una causa raíz no anotada hasta ahora: **el sistema insertaba registros en la colección `notificaciones` pero no existía ningún endpoint ni pantalla para verlos** — ni una campana, ni una bandeja. Antes de cerrar HU-02/HU-07 con propiedad hacía falta esa pieza, así que este bloque también la construyó:

11. **Bandeja de notificaciones real** (`backend/src/routes/notificaciones.routes.js`, `frontend/public/js/modules/notificaciones.js`): campana en el topbar con contador de no leídas, dropdown con las notificaciones reales del usuario (acotadas por rol + contratos donde participa), marcar una o todas como leídas, poll cada 30s. Reutilizada por HU-02, HU-07, HU-13 y HU-20 (las notificaciones que ya se insertaban desde antes ahora son visibles).
12. ~~**HU-02**: alerta real de vencimiento de fianza a 30/15/5 días~~ — hecho, job en `backend/src/jobs/alertasScheduler.js` (`checkFianzasVigencia`), corre al arrancar + cada hora + al vuelo tras registrar fianza/endoso.
13. ~~**HU-07**: disparo real de la alerta de atraso + entrega por canal~~ — hecho, `checkAlertasConcepto` evaluado tras cada evento que cambia el avance real (trabajos por periodo, integración de estimación); canal `correo` va a un buzón simulado (`correos_salientes`) separado de la campana de `sistema`.
14. ~~**HU-09**: formato estructurado "dice / debe decir"~~ — hecho, checkbox dedicado en el formulario de nota + autocompletado del texto original + validación backend contra la nota vinculada.
15. ~~**HU-04**: descarga por fila en el buscador del expediente~~ — hecho, campo `archivo` resuelto por bloque en `GET /expediente/search`.

Verificado en navegador (12/12): campana con contador real (3 notificaciones por una fianza a 3 días de vencer), marcar como leída actualiza el contador, alerta con canal `correo` no contamina la campana de `sistema` del residente, panel de alertas de concepto muestra "ALERTA DISPARADA" con el avance real evaluado (evento de backend, ya no una heurística visual del cliente), formulario de corrección con autocompletado de "Dice" y validación, nota de corrección visible con su badge dedicado, y enlace de descarga real por fila en el buscador del expediente (probado con una fianza sin archivo y una minuta con PDF real). Las 9 suites de regresión previas (`verify.js`, `verify_fixes.js`, HU-10, HU-17, HU-18, HU-19, Bloque 1, Bloque 2, Bloque 4 — 108 checks) se re-corrieron sobre el código final sin hallazgos nuevos.

## Estado general

Las 24 historias de usuario del Excel están en estado **Cumple**. El núcleo transaccional (contratos, fianzas, convenios, bitácora, programa, trabajos por periodo, integración/pago de estimaciones) funciona y está modularizado por dominio en backend (`src/routes/`) y frontend (`js/modules/`). La capa de "evidencia y trazabilidad legal" alrededor de ese núcleo también es real: notificaciones visibles en una campana propia (no solo registros invisibles en la base de datos), semáforos de plazo calculados contra fechas reales, exportaciones reales (XLSX/PDF), alertas de vencimiento y de atraso disparadas por eventos reales (no heurísticas de UI), y búsqueda/descarga documental con lógica AND.

Dos decisiones de diseño quedan anotadas por transparencia, sin que bloqueen ningún criterio de aceptación: (a) el expediente de HU-04 presenta sus 5 bloques repartidos en pestañas en vez de una sola vista continua — el buscador global sí los cruza a todos; (b) el canal "correo" de HU-07 entrega a un buzón simulado dentro de la app (`correos_salientes`) en vez de una bandeja SMTP real, para no depender de credenciales externas no provistas por el usuario.
